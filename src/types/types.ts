export type AccidentReport = {
    accidentTime: string
    address: string
    createdAt: string
    isActive: boolean
    isResolved: boolean
    latitude: string
    longitude: string
    severity: string
}